# HealthPredict ML Service

Flask microservice for disease prediction, OCR, report analysis, doctor booking, and related health APIs.

## Render

Use the repository root `render.yaml` Blueprint. The ML service root directory is `ml_service`, and the service pins Python 3.11 to keep the pinned scientific Python dependencies installable.
